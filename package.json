{
  "name": "@hypercolor/promise-queue",
  "version": "0.0.2",
  "description": "Used by the Hypercolor Digital team to support concurrent looping over an array. Supports a concurrency count to control how many promises may be resolved at once",
  "main": "index.js",
  "types": "index.d.ts",
  "files": [
    "/**/*.d.ts",
    "/**/*.js",
    "index.js",
    "index.d.ts",
    "LICENSE"
  ],
  "private": false,
  "prepublish": "tsc --build --clean && tsc --build",
  "scripts": {
    "build": "tsc --build --clean && tsc --build"
  },
  "keywords": [
    "promise",
    "queue",
    "async",
    "loop",
    "concurrent",
    "loop"
  ],
  "devDependencies": {
    "@types/p-queue": "^3.2.1",
    "ts-loader": "^9.4.2",
    "tslint": "^6.1.3",
    "tslint-loader": "^3.5.4",
    "typescript": "^4.9.4"
  },
  "author": "Seth Uschuk",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hypercolor/promise-queue.git"
  },
  "bugs": {
    "url": "https://github.com/hypercolor/promise-queue/issues"
  },
  "homepage": "https://github.com/hypercolor/promise-queue#readme",
  "dependencies": {
    "p-queue": "^7.3.4"
  }
}
